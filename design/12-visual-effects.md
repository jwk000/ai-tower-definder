# 12 — 视觉特效

> PixiJS WebGL 渲染 | 复合几何体美术 | 以符号和动态代替文字

---

## 1. 设计原则

| 原则 | 说明 |
|------|------|
| **形状即身份** | 每个单位有独特的**复合几何体轮廓**和符号特征组合，一眼可辨（详细规范见 [16-美术资产设计 §3](./16-art-assets-design.md#三复合几何体系统)） |
| **动效即信息** | 不需要文字，攻击动画/待机动画/死亡动画表达单位功能 |
| **色彩即阵营** | 蓝色系=我方，红色系=敌方，黄色/青色=中立 |
| **粒子即反馈** | 命中/死亡/建造/升级用粒子特效传达，不用弹窗文字 |
| **减少文字** | 场景中不显示单位名称标签。仅在UI面板中按需显示信息 |

### 1.1 识别度铁律（P0-#2 修复）

> **零文字标签**的代价是：所有信息必须通过形状/颜色/动作传达。这要求识别度设计必须经得起严格测试。

| 测试项 | 标准 | 验证方式 |
|--------|------|---------|
| **敌人轮廓识别** | 60px 缩略尺寸下，玩家 2 秒内能分类 9 种敌人（小兵/快兵/重装/法师/自爆虫/Boss×2/飞行/分散） | 详见 [16-art-assets §5.3.0 敌人轮廓识别度矩阵](./16-art-assets-design.md#530-敌人轮廓识别度矩阵p0-2-修复) |
| **我方单位识别** | 40px 缩略尺寸下移动中，玩家 2 秒内能识别 6 种我方单位 | 详见 [16-art-assets §5.2.0 我方单位轮廓识别度矩阵](./16-art-assets-design.md#520-我方单位轮廓识别度矩阵p0-2-修复) |
| **塔等级识别** | 不依赖数小菱形，通过塔底纹章颜色（铜/银/金/星形）1 秒内识别 L2~L5 | 详见 [16-art-assets §12.1.1b 多维等级标识系统](./16-art-assets-design.md#1211b-多维等级标识系统p0-2-修复) |
| **状态识别** | 眩晕/冰冻/嘲讽/燃烧等状态在身上 0.5 秒内可辨 | §5.2 状态反馈 |

> **如果设计违反铁律，视为 BUG**。视觉迭代时如有"看起来都差不多"的反馈，必须依照矩阵补充独家特征。

---

## 2. 渲染架构

> **v2.3 更新**：EntityLayer 包含实体主体 + 头顶 HUD（血条/名字/等级），渲染逻辑在 RenderSystem 中按实体逐行处理。

```
PixiJS Stage
├── BackgroundLayer      背景底色
├── MapLayer            地图网格（tile）+ 场景装饰
├── EntityLayer         实体图形（按Y排序）+ 头顶HUD（血条/名字/等级）
├── ProjectileLayer     弹道
├── EffectLayer        粒子特效（爆炸/天气/技能）
├── UILayer            HUD + 工具栏 + 选中信息面板
└── OverlayLayer       暂停/结算
```

---

## 3. 单位视觉语言

### 3.1 塔类 — 复合几何体（以结构和弹道识别）

> 详细复合几何体规范见 [16-美术资产设计 §5.1](./16-art-assets-design.md#51-塔类--6种)。

| 塔 | 复合几何体结构 | 符号特征 | 弹道特效 | 一眼识别点 |
|----|----------------|----------|----------|------------|
| **箭塔** | 基座矩形 + 细长塔身矩形 + 顶部箭头三角 + 射击口矩形 + 弓臂弧线(高等级) | 塔身纤细，顶部尖锥指向天空，等级菱形标识 | 蓝色细长箭头飞行，带尾迹粒子 | 高瘦塔形 + 蓝色箭头连射 |
| **炮塔** | 基座矩形 + 短粗塔身矩形 + 圆形炮口+炮膛 + 加固钢环 + 铆钉装饰 | 炮口有后坐力动画（攻击时向后微退），等级菱形 | 橙色圆弹抛出弧线，命中时爆炸扩散 | 矮胖塔形 + 橙色炮弹 + 爆炸 |
| **冰塔** | 基座矩形 + 六边形冰晶核心 + 内切菱形 + 4-16个小冰晶环绕 + 六边冰环 | 冰晶缓慢旋转，散发冷光粒子，等级菱形 | 蓝色菱形冰晶飞行，命中时白色冰雾扩散 | 旋转冰晶 + 蓝色菱形 + 雾 |
| **电塔** | 基座矩形 + 线圈主体矩形 + 3-8圈金色线圈环 + 1-5个放电针三角 + 电极球 | 线圈间有微小电弧跳动（待机动画），等级菱形 | 黄色闪电瞬间命中（非弹道），带分支电弧 | 线圈 + 电弧跳动 + 闪电链 |
| **激光塔** | 基座矩形 + 中央晶体柱矩形 + 聚焦透镜菱形 + 1-4个副晶体柱 + 多层辉光 | 晶体柱持续发光脉动（明暗呼吸），等级菱形 | 青色光束贯穿（细长矩形），持续0.15s | 发光晶体 + 青色贯穿光束 |
| **蝙蝠塔** | 基座矩形 + 塔柱矩形 + 2-8个蝙蝠翼 + 蝙蝠身圆 + 哥特塔尖三角 + 暗影光环 | 休眠时蝙蝠翼收起（灰色），激活时展开+紫色暗影粒子，等级菱形 | 紫色蝙蝠影飞向目标，命中后飞回（吸血） | 蝙蝠翼展开/收起 + 紫色飞行影子 |

### 3.2 我方移动单位 — 复合几何体（以移动方式和攻击动作识别）

> 详细复合几何体规范见 [16-美术资产设计 §5.2](./16-art-assets-design.md#52-我方移动单位--6种)。

| 单位 | 复合几何体结构 | 符号特征 | 攻击动画 | 一眼识别点 |
|------|----------------|----------|----------|------------|
| **盾卫** | 身体矩形 + 前方盾牌(半圆弧) + 盾徽圆 + 脚下阴影椭圆 | 移动缓慢稳重，待机时盾牌有微光呼吸 | 盾牌撞击（短距前冲 + 震荡波） | 大盾牌 + 缓慢移动 |
| **剑士** | 身体矩形 + 剑刃矩形+剑尖三角 + 剑柄矩形 + 脚下阴影椭圆 | 移动中等速度，待机时剑有寒光闪烁 | 快速旋转（旋风斩动画：旋转 + 白色弧线扩散） | 剑形 + 旋转攻击 |
| **弓手** | 身体矩形 + 弓弧(弧线) + 弓弦线 + 箭袋矩形 + 脚下阴影椭圆 | 移动时有拉弓蓄力姿态 | 拉弓→放箭（箭矢比其他弹道更长更细） | 弓形 + 细长箭 |
| **祭司** | 身体矩形 + 头顶光环(圆环) + 3个光点漂浮 + 法杖矩形+杖顶宝石圆 + 脚下阴影椭圆 | 光环旋转 + 周围有微弱光点漂浮 | 光环扩散脉冲（治疗波从自身扩散到目标） | 圆环光环 + 脉冲波 |
| **工程师** | 身体矩形 + 扳手交叉符号 + 2个旋转齿轮 + 安全帽矩形 + 脚下阴影椭圆 | 待机时有小齿轮粒子绕身 | 锤击动画 + 目标建筑短暂金色闪烁 | 扳手 + 修复金光 |
| **刺客** | 身体矩形 + 匕首(细长三角) + 眼部面罩矩形 + 残影矩形×2 + 脚下阴影椭圆 | 移动速度快，身后有短残影拖尾 | 瞬移（消失→在目标旁出现+红色X斩击线） | 匕首 + 瞬移残影 |

### 3.3 敌方单位 — 复合几何体（以体型和移动特征识别）

| 敌人 | 复合几何体结构 | 符号特征 | 移动特征 | 一眼识别点 |
|------|----------------|----------|----------|------------|
| **小兵** | 身体圆 r=10 + 白色双眼(圆×2) + 瞳孔(圆×2) + 深色描边 | 红色实心主体，圆形轮廓 | 匀速沿路走，无特殊移动 | 红圆+白眼，最基础外形 |
| **快兵** | 横向椭圆体 + 身后2条速度线矩形 + 白色单眼 | 横向拉伸椭圆 + 速度线拖尾 | 快速移动，身后拖速度线残影 | 椭圆 + 速度线 + 快 |
| **重装兵** | 大圆 r=16 + 3px厚灰色铠甲描边 + 4个铆钉圆 + 黄色怒目 | 厚重铠甲描边 + 铆钉装饰 | 缓慢沉重，每步微小震动 | 大圆厚甲 + 铆钉 + 慢 |
| **法师** | 六边形体 + 3个魔法粒子绕转 + 法杖(细矩形+顶宝石圆) + 兜帽三角 | 六边形+环绕粒子+三角兜帽 | 中等速度，法杖高举姿态 | 六边形 + 环绕粒子 + 法杖 |
| **自爆虫** | 锯齿圆体 + 8个外围三角尖刺 + 内核脉动圆 + 不规则描边 | 不规则的闪烁轮廓，像燃烧 | 快速移动 + 身体不规则脉动膨胀收缩 | 锯齿闪烁 + 脉动 + 快 |
| **Boss指挥官** | 超大体圆 r=24 + 头顶三三角皇冠 + 脚下脉动光环 + 黄色怒目 + 背后三角披风 | 金色描边 + 大型皇冠 + 光环 + 大血条 | 慢速沉稳，身后有跟随小兵 | 大金圆 + 皇冠 + 光环 |
| **Boss攻城兽** | 超大圆 r=28 + 背上4根三角尖刺 + 4px暗色厚甲描边 + 小黄眼 + 地面裂纹线 | 深橙厚甲 + 背部尖刺 + 地面裂纹 | 极慢，每步有裂纹扩散 | 尖刺巨兽 + 地面裂纹 |
| **飞行敌**（v1.1，L4 引入） | 小圆体 r=8 + 两侧三角翅膀（拍动动画） + 中央小红点眼 + **脚下无影**（替换为下方淡淡 alpha=0.15 椭圆"飞影"，半径 12px） | 持续上下浮动（±8px，频率 4Hz） + 翅膀快速拍动（每秒 6-10 帧切换） + **整体悬浮于 y-12px**（视觉与地面分离） | 沿路径前进 + 浮动叠加 | **拍翅膀的小蝙蝠形 + 悬浮无脚** —— 一眼看出"不能用地面爆炸打" |
| **分散敌**（v1.1，L6 引入） | 主体圆 r=10 + 4-6 条蠕动触手（贝塞尔曲线，每 0.5s 重新生成形状） + 中央双眼 | 触手周期性"散开 + 聚拢"（1.5s 周期，伸出方向随机） + **移动中身体在路径垂直方向 ±20~30px 偏移** | 表面是匀速前进，但在路径上画 S 形 | **触手不停摆动 + 身体在路径上左右晃** —— 一眼看出"密度评分难"|

> **详细复合几何体规范**见 [16-美术资产设计 §5.3](./16-art-assets-design.md#53-敌方单位--7种)。|

#### 飞行敌视觉关键点（v1.1）

- **悬浮视觉**：飞行敌渲染时 y 坐标 = 实际逻辑 y - 12px（视觉上"漂浮在路径上方"）
- **下方飞影**：替换地面阴影为虚化的椭圆（alpha 0.15，模拟"高空中投下的影子"），玩家潜意识识别"这是空中目标"
- **拍翅膀**：翅膀矩形每 6-10 帧切换角度（±25°），形成扑翼动画
- **特殊调试视觉**: 选中带"`cantTargetFlying: true`"标签的塔（如导弹塔）时，**飞行敌头顶显示禁止符号** ⊘（红色斜杠圆圈），明确告知"此塔打不到我"

#### 分散敌视觉关键点（v1.1）

- **触手摆动**：4~6 条触手（颜色：暗紫 `#7b1fa2`），每条由 3 个控制点的贝塞尔曲线绘制
- **路径扰动可视化**：分散敌实际位置可能偏离路径中心 ±20~30px，路径上会留下短暂的紫色拖尾粒子（0.3s 衰减），强化"散开"视觉
- **聚拢瞬间**：每 1.5s 周期的"聚拢瞬间"触手向内收拢，形成短暂的"球状"——玩家学会"在聚拢的瞬间开导弹"，但塔的 AI 无法预测这种节奏，强化"导弹塔失效"的体验

### 3.4 中立/建筑 — 以结构和状态识别

| 单位 | 识别点 |
|------|--------|
| **金矿** | 金色方形 + 顶部金币符号持续闪烁 + 金币粒子间歇飘出 |
| **能量塔** | 紫色方形 + 顶部能量球体脉动 + 蓝色粒子上升 |
| **尖刺陷阱** | 地面三个红色三角聚簇 + 触发时闪红扩张 |
| **治疗泉水** | 青色六边形 + 中心波纹持续扩散（水波效果） |
| **金币宝箱** | 金色矩形 + 间歇金色闪光 |
| **基地** | 大型蓝色六边形 + 护盾光环脉动 |
| **出生点** | 红色矩形 + 敌人出现时扩散波 |

---

## 4. 弹道特效（无文字，纯视觉）

| 塔类型 | 弹道视觉 | 命中特效 |
|--------|----------|----------|
| 箭塔 | 蓝色细长箭头（杆+三角头），尾迹3个粒子 | 白色小星爆（4-6个粒子） |
| 炮塔 | 橙色圆球，弧线轨迹 | 橙红色圆形爆炸扩散 + 屏幕微震 |
| 冰塔 | 蓝色小菱形，直飞，尾迹冰晶粒子 | 白色冰雾圆形扩散（冻结时敌人变冰蓝） |
| 电塔 | 黄色锯齿闪电（0.05s内瞬达），分支电弧 | 目标闪黄 + 电弧跳跃到下一个目标 |
| 激光塔 | 青色细长光束（持续0.15s），渐变淡出 | 敌人短暂发青光 |
| 蝙蝠塔 | 紫色蝙蝠形影子（有翅膀拍动动画），飞弧线 | 绿色微光回流（吸血反馈） |
| **导弹塔** | **红色大型弹体（24~32px）+ 橙黄渐变尾焰粒子**，抛物线弹道（先向上后落下），头部沿速度方向旋转 | **红色冲击波环 + 蘑菇云 + 棋盘震动 + 地格破损**（详见 [19-导弹塔 §4.5~4.7](./19-missile-tower.md#四攻击序列完整流程)） |

### 4.1 导弹塔射程圈可视化（v1.1）

> 导弹塔射程从 9999（全图）削弱到 600px 后，**射程圈可视化变为关键反馈**——玩家必须在建造时清楚看到 600px 的影响范围，避免建错位置。

| 场景 | 视觉表现 |
|------|---------|
| **建造预览**（拖拽建造按钮到地图） | 鼠标位置显示 600px 半径的红色虚线圆环（线宽 2px，alpha 0.6，dash pattern 8-4） |
| **选中已建塔** | 显示 600px 红色实线圆环（线宽 2px，alpha 0.4），与其他塔射程圈样式一致 |
| **塔射程内地格高亮** | 选中导弹塔时，射程内的可达地格底色微微变红（alpha 0.08），帮助玩家理解"哪些格子是这个塔的攻击范围" |
| **目标预览**（鼠标悬停在导弹塔上） | 在射程内评分最高的目标地格显示红色十字准星，并显示评分数值（小型 HUD） |

---

## 5. 粒子特效清单

### 5.1 战斗反馈

| 特效 | 触发 | 视觉表现 | 目的 |
|------|------|----------|------|
| **命中** | 弹道到达目标 | 弹道颜色的小圆点飞散 6-10个 | 攻击命中确认 |
| **暴击** | 暴击命中 | 加大粒子 + 轻微白色闪光 | 更高伤害反馈 |
| **爆炸** | 炮塔/自爆虫 | 红色+橙色圆点爆炸扩散 15-25个 + 快速缩放消失 | AOE伤害范围 |
| **死亡** | 单位HP归零 | 单位颜色粒子收缩→爆散 8-12个 | 击杀确认 |
| **BOSS死亡** | Boss HP归零 | 金色大爆炸 + 全场0.3s闪白 + 慢动作 | 重要事件 |

### 5.2 状态反馈

| 特效 | 触发 | 视觉表现 |
|------|------|----------|
| **眩晕** | 炮塔命中 | 目标金色tint闪烁 + 头顶黄色星形符号旋转 |
| **减速** | 冰塔命中 | 目标颜色向蓝色渐变 + 脚下蓝色冰面纹 |
| **冰冻** | 冰塔满层 | 目标变亮青色 + 外包冰晶轮廓线 + 完全静止 |
| **嘲讽** | 盾卫技能 | 被嘲讽的敌人红色描边变粗 + 指向盾卫的虚线 |
| **受击** | 任何伤害 | 100ms纯白tint（闪白） |
| **飞行无效**（v1.1） | 导弹爆炸在飞行敌附近但不命中 | 飞行敌头顶短暂浮起 ⊘ 红色禁止符号（0.3s alpha 渐隐），明确告知玩家"地面爆炸打不到" |
| **L5 热压**（v1.1） | 导弹塔 L5 爆炸中心 10% 半径内有敌人 | **中心区域呈现亮白色高温球**（半径 = 爆炸半径 × 10%，alpha 1.0→0.5），中心敌人头顶浮起 "+20%" 文字粒子（0.5s 上升淡出） |

### 5.3 经济反馈

| 特效 | 触发 | 视觉表现 |
|------|------|----------|
| **+金币** | 击杀敌人 | 金色小圆点从尸体位置飞向HUD金币区 |
| **+能量** | 击杀精英/Boss | 蓝色小圆点飞向HUD能量区 |
| **建造完成** | 放置建筑 | 金色方块粒子从基座上升 |
| **升级完成** | 建筑升级 | 白色+金色光芒粒子上升 + 建筑短暂放大 |
| **回收** | 单位出售 | 灰色方块向下消散 + 金币粒子飞回HUD |

### 5.4 天气粒子

| 天气 | 粒子表现 |
|------|----------|
| **下雨** | 蓝色细长矩形（2×8）持续下落，400-600px/s |
| **下雪** | 白色小圆（r=2-4）缓慢飘落 + 随机横向摆动 |
| **下雾** | 大型白色半透明圆（r=40-80）缓慢漂移，alpha 0.05-0.1 |
| **夜晚** | 路径微光点（r=3，alpha 0.2-0.4）缓慢明灭 |

---

## 6. 动画系统

### 6.1 待机动画（让场景不僵硬）

| 单位 | 待机动画 |
|------|----------|
| 塔 | 微小上下浮动（±1px正弦波），攻击塔有微小旋转或瞄准漂移 |
| 我方单位 | 微小左右摇摆（待机呼吸感） |
| 敌人行走 | 沿路径移动时身体有微小弹跳（±2px），模拟步行 |

### 6.2 过渡动画

| 场景 | 动画 |
|------|------|
| 建造 | scale 0→1，0.2s弹性缓出 |
| 死亡 | scale 1→0 + alpha 1→0，0.3s |
| Boss二阶段 | 0.5s白/红闪烁 + scale从1→1.3弹性扩张 + 0.3s全屏暂停 |
| 天气切换 | 粒子淡出→淡入，画面色调渐变，1.5s |
| 关卡进入 | 黑色遮罩alpha 1→0，0.5s |

---

## 7. UI面板（按需显示文字 + 实体头顶 HUD）

> **v2.3 更新**：场景中每个实体头顶始终显示血条/名字/等级 HUD（详见 [09-UI/UX设计 §6.1](./09-ui-ux.md#61-实体头顶-hud-规范)）。选中实体时，底部额外弹出详情面板。

实体头顶 HUD（始终可见）:
```
         ◆ ◆ ◆          ← 等级指示器
        箭塔              ← 名字标签
   ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓     ← 血条
    ╔══════════╗
    ║  实体图形  ║
    ╚══════════╝
```

选中实体时，底部显示信息面板：
```
┌─────────────────────────────────────────────────────┐
│  [图标] 箭塔  Lv.3      ATK:18  范围:240  攻速:1.0/s │
│         HP:100/100      升级费:110G   [升级] [回收]   │
└─────────────────────────────────────────────────────┘
```

- 实体头顶 HUD 始终可见：血条（含数值）、名字、等级
- 选中时额外显示底部详情面板
- 顶部 HUD 保留资源/波次等战略数字

---

## 8. 性能目标

| 指标 | 目标 |
|------|------|
| 稳定帧率 | 60fps |
| 同屏实体上限 | 200+ |
| 同屏粒子 | 500+（ParticleContainer优化） |
| 最低帧率 | 30fps（低端设备） |

---

## 9. 蝙蝠塔攻击动作设计

> 蝙蝠塔攻击由独立的**蝙蝠群成员**（Bat Swarm Member）实体执行。每只蝙蝠在攻击敌人时需展现完整的生物攻击行为——锁定目标、俯冲、撕咬、吸血回弹。本章定义攻击动作的阶段划分、视觉规范、状态机扩展及代码集成要点。

### 9.1 攻击阶段划分

蝙蝠攻击动作分为**四个阶段**，总持续时间 = 攻击间隔（`1 / attackSpeed ≈ 1.33s`，L1 攻速 0.75/s）：

```
┌──────────────┬──────────────┬──────────────┬──────────────┐
│   锁定蓄力    │   俯冲攻击    │   撕咬命中    │   回弹归巢    │
│  (0% - 20%)  │  (20% - 60%) │  (60% - 75%) │  (75% - 100%)│
│    ~0.27s    │    ~0.53s    │    ~0.20s    │    ~0.33s    │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

**状态机**：`IDLE ⇄ ATTACK(Lock → Swoop → Bite → Return) → IDLE`

攻击动作**不可打断**（除蝙蝠死亡外），且优先级高于 boid 集群运动——攻击中蝙蝠沿预设轨迹飞行，不参与 boid 位置计算。

### 9.2 各阶段视觉规范

#### 阶段1：锁定蓄力（Lock）`phase 0.00 – 0.20`

蝙蝠在原位蓄力，锁定目标。

| 参数 | 过渡 | 说明 |
|------|------|------|
| 翅膀拍打频率 | 3-6 Hz → 8-10 Hz | 逐渐加速 |
| 翅膀展开角度 | 30°-70° → 45°-85° | 翼展逐步增大 |
| 身体朝向 | 旋转对准目标 | `rotation` 指向 target |
| 身体 alpha | 0.85 → 0.65 | 蓄力暗影感（加深紫色） |
| 凝聚粒子 | 3-5 个暗影粒子向身体聚拢 | `#7b1fa2`, r=2-3, alpha 0→0.4 |

**凝聚粒子实现**：每帧 lerp 粒子位置向蝙蝠身体中心靠拢，到达后随阶段切换消散。

#### 阶段2：俯冲攻击（Swoop）`phase 0.20 – 0.60`

蝙蝠沿弧线轨迹高速俯冲向目标。

| 参数 | 值 |
|------|-----|
| 飞行轨迹 | 二次贝塞尔曲线：起点（蝙蝠位置）→ 弧顶（抬高 15-30px）→ 目标位置 |
| 翅膀拍打频率 | 8-12 Hz 高频 |
| 翅膀展开角度 | 45°-85° 大幅拍打 |
| 身体 scale | 1.0 → 1.15（俯冲加速感） |
| 位移速度 | 临时提升至 `batSpeed × 1.5` |
| 拖尾粒子 | 每 0.05s 在身后生成 1 个暗影粒子（size 3→1, alpha 0.5→0, life 0.3s） |
| 颜色 | `#7c4dff`（紫） |

**俯冲贝塞尔曲线**：

```typescript
function getSwoopPosition(t: number, start: Vec2, target: Vec2, arcHeight: number): Vec2 {
  const midX = (start.x + target.x) / 2;
  const midY = Math.min(start.y, target.y) - arcHeight;
  // 二次贝塞尔：B(t) = (1-t)²P₀ + 2(1-t)tP₁ + t²P₂
  return {
    x: (1-t)*(1-t)*start.x + 2*(1-t)*t*midX + t*t*target.x,
    y: (1-t)*(1-t)*start.y + 2*(1-t)*t*midY + t*t*target.y,
  };
}
```

#### 阶段3：撕咬命中（Bite）`phase 0.60 – 0.75`

蝙蝠抵达目标，执行撕咬——**此阶段造成伤害并吸血**。

| 参数 | 值 | 说明 |
|------|-----|------|
| 翅膀展开角度 | 瞬间 90° | 爆发姿态 |
| 身体 scale | 1.15 → 0.9 | 前冲→弹回 |
| 獠牙图形 | 2 个红色三角（`#ff1744`, size=3） | 仅此阶段在嘴部绘制 |
| 命中粒子 | 5-8 个紫色粒子爆散（r=2-4, v=60-120px/s, life 0.3s） | 目标位置 |
| 吸血绿光 | 3-5 个绿色光点（`#69f0ae`, r=2-3）从目标飞回蝙蝠身体 | 持续 0.2s |
| 受击闪白 | `hitFlashTimer = 0.1s` | 已有机制 |
| 屏幕微震 | `amplitude=2, duration=0.05s` | 增强打击感 |

**伤害时机**：`phase = 0.65` 时调用 `applyDamageToTarget()`（咬合瞬间），而非攻击起始。

#### 阶段4：回弹归巢（Return）`phase 0.75 – 1.00`

蝙蝠从目标回弹，返回 boid 集群位置，恢复正常。

| 参数 | 过渡 | 说明 |
|------|------|------|
| 飞行轨迹 | 目标 → boid 归巢点 | 直线或微弧（弧高 5-10px） |
| 翅膀拍打频率 | 8-10 Hz → 3-6 Hz | 逐渐减速归常 |
| 翅膀展开角度 | 85° → 30°-70° | 逐步回缩 |
| 身体 scale | 0.9 → 1.0 | 弹回复原 |
| 吸血余光 | 身体叠加绿色辉光（`#69f0ae`, alpha 0.2→0, 0.3s） | 吸血成功反馈 |
| 拖尾粒子 | 无 | 回程简洁 |

### 9.3 动画状态扩展

在 `BatSwarmSystem` 内部 `batAnimStates` Map 中新增以下字段：

```typescript
// 攻击状态（追加到现有 batAnimState）
{
  attackPhase:      number;   // 0-1 攻击进度（0=未攻击/待机）
  isAttacking:      boolean;  // 是否正在执行攻击动作
  attackTargetId:   number;   // 当前攻击目标 entity ID
  attackTargetX:    number;   // 目标 X（攻击启动时快照）
  attackTargetY:    number;   // 目标 Y
  swoopStartX:      number;   // 俯冲起点 X
  swoopStartY:      number;   // 俯冲起点 Y
  swoopArcHeight:   number;   // 俯冲弧高（15-30 随机）
  attackFlapSpeed:  number;   // 攻击加速拍打频率
  boidReturnX:      number;   // 回弹归巢点 X（boid 位置快照）
  boidReturnY:      number;   // 回弹归巢点 Y
}

// 阶段阈值
const PHASE_LOCK_END   = 0.20;
const PHASE_SWOOP_END  = 0.60;
const PHASE_BITE_END   = 0.75;
```

### 9.4 代码集成要点

**触发点**：`BatSwarmSystem.tryAttack()` 末尾新增攻击状态设置：

```typescript
// tryAttack() 中找到目标并 applyDamageToTarget() 后：
const anim = batAnimStates.get(batId);
if (anim) {
  anim.isAttacking = true;
  anim.attackPhase = 0;
  anim.attackTargetId = targetId;
  anim.attackTargetX = Position.x[targetId];
  anim.attackTargetY = Position.y[targetId];
  anim.swoopStartX = Position.x[batId];
  anim.swoopStartY = Position.y[batId];
  anim.swoopArcHeight = 15 + Math.random() * 15;  // 15-30px 随机弧高
  anim.boidReturnX = boidTargetX;  // boid 集群计算的归巢位置
  anim.boidReturnY = boidTargetY;
  anim.attackFlapSpeed = anim.flapSpeed * 2.2;    // 加速至约 8-10 Hz
}
```

**注意**：原有 `applyDamageToTarget()` 调用需**延迟**到 `phase = 0.65`（撕咬命中瞬间），而非攻击启动即刻造成伤害。这需要修改伤害时机——在动画更新循环中检测 phase 跨越 0.65 阈值时执行。

**位置覆盖**：攻击期间用预设轨迹覆盖 boid 位置计算：

```typescript
if (anim.isAttacking && anim.attackPhase < 1) {
  const p = anim.attackPhase;
  if (p <= PHASE_LOCK_END) {
    // 锁定：保持原位，仅更新朝向
  } else if (p <= PHASE_SWOOP_END) {
    // 俯冲：贝塞尔曲线
    const t = (p - PHASE_LOCK_END) / (PHASE_SWOOP_END - PHASE_LOCK_END);
    const pos = getSwoopPosition(t, swoopStart, target, anim.swoopArcHeight);
    Position.x[batId] = pos.x;
    Position.y[batId] = pos.y;
  } else if (p <= PHASE_BITE_END) {
    // 撕咬：锁定在目标位置
    Position.x[batId] = anim.attackTargetX;
    Position.y[batId] = anim.attackTargetY;
  } else {
    // 回弹：lerp 至归巢点
    const t = (p - PHASE_BITE_END) / (1 - PHASE_BITE_END);
    Position.x[batId] = lerp(anim.attackTargetX, anim.boidReturnX, t);
    Position.y[batId] = lerp(anim.attackTargetY, anim.boidReturnY, t);
  }
}
```

**渲染改动**：`BatSwarmSystem.renderBat()` 根据 `attackPhase` 调整绘制参数：

| 阶段 | wingAngle 系数 | bodyScale | 额外图形 |
|------|---------------|-----------|---------|
| 待机 | 1.0 | 1.0 | 无 |
| 锁定 | 1.0→1.3 | 1.0 | 凝聚粒子 |
| 俯冲 | 1.3→1.5 | 1.0→1.15 | 拖尾粒子 |
| 撕咬 | 1.5→1.8→1.5 | 1.15→0.9 | 獠牙三角×2 + 命中粒子 |
| 回弹 | 1.5→1.0 | 0.9→1.0 | 绿色吸血辉光 |

**攻击完成重置**：`attackPhase ≥ 1.0` 时：
```typescript
anim.isAttacking = false;
anim.attackPhase = 0;
anim.flapSpeed = baselineFlapSpeed; // 恢复 3-6 Hz
```

### 9.5 性能考量

| 指标 | 预估值 |
|------|--------|
| 单塔最大攻击蝙蝠数 | 4-6 只（L1=4, L3=5, L5=6） |
| 峰值并发拖尾粒子 | ≤ 48 个（6 只 × 8 粒子） |
| 峰值并发命中粒子 | ≤ 48 个（6 只 × 8 粒子） |
| 贝塞尔计算 | ≤ 6 次/帧，CPU 可忽略 |
| 预计帧开销 | < 0.1ms/帧 |

粒子使用现有 `ParticleContainer` 或复用 `BloodParticleSystem` 粒子池，避免逐粒子 Draw Call。
