# 开发日志 — 2026-05-14

> 主题：塔升级模型重构 v3.1 —— 关外卡池科技树（设计阶段，仅文档）
> 设计权威：[30-tower-tech-tree.md](../30-tower-tech-tree.md)（新建）
> 影响文档：03 / 04 / 13 / 19 / 21 / 22 / 25 / README

---

## 一、今日完成

把塔升级从"关内线性 L1-L5"重构为"关外卡池科技树（路径互斥 + 节点线性解锁）"。本次为纯设计文档变更，**未触碰代码与测试**，按 AGENTS.md「设计先行」原则推进。

### 1.1 需求确认（用户三轮对话）

用户原始诉求要点：

- 关内不再升级，升级移到关卡外卡池界面。
- 升级不是线性，而是科技树（多路径），每塔 1-3 条路径。
- 七塔的具体路径：箭塔/炮塔/电塔/激光塔/蝙蝠塔/导弹塔/冰塔（→ 元素塔）。
- 元素塔默认冰形态，3 条路径覆盖冰/火/毒。
- 毒藤塔、弩炮塔整体废弃。

我评估并回复 6 个关键设计点：

1. **基础节点是否共享** —— 用户同意「单基础塔卡 + 两路径分支」，避免卡池爆炸。
2. **关内临时升级是否保留** —— 用户决定保留 `instanceLevel`（仅本局、塔死亡清零、不带出关外）。
3. **路径切换粒度** —— 用户同意"关外可切换，关内不可"。
4. **弩炮塔穿透归宿** —— 用户决定承接到炮塔路径 2 终点（战术炮塔 = 狙击 + 穿透，去掉眩晕 Boss）。
5. **真火塔能量丹** —— 用户同意复用 `EnergySystem`（击杀概率 +1 E，不新增资源）。
6. **导弹分裂语义** —— 用户改为"同地格多发射，单发伤害↓总伤害↑"。

用户最后回复"确认修改"，进入实施阶段。

### 1.2 新建 design/30-tower-tech-tree.md（commit 1）

396 行的科技树权威文档，含 14 节：

- §1 模型概述、§2 全局规则（路径互斥+节点线性+本局装备+关内禁切+路径重置）
- §3 字段约定（YAML + 存档 + 配置接口）
- §4 七塔完整科技树（17 条路径 / 23 个节点）
- §5 路径终点能力承接（弩炮→战术炮塔 / 毒藤→病毒塔 / 真火→能量丹）
- §6 UI 草图、§7 影响文档清单、§8 废弃单位清单
- §9 开放问题（5 个待定）、§10 数据迁移、§11 术语表

### 1.3 同步 22-new-unit-design.md v1.2（commit 2）

- 顶部加 v3.1 变更声明
- §4.1 毒藤塔 / §4.3 弩炮塔标 DEPRECATED
- §7.1/§7.2/§7.3 阵容表移除两塔
- §10 路线图：毒藤承接给病毒塔、弩炮承接给战术炮塔

### 1.4 同步 03-unit-data.md v3.1（commit 3）

- 顶部 v3.1 变更声明
- §1 塔表：删除 L3 被动列、新增"科技树路径"列；`ice_tower` 全表改名 `elemental_tower`
- §1.3 新增"塔科技树字段规范"小节
- §8.1 卡 ID 索引 `ice_tower_card` → `elemental_tower_card`
- §10 字段表：`instanceLevel` 语义收窄、`baseLevel` 标 @deprecated、新增 `techTree.pathDepth` / `equippedPath`

### 1.5 同步 13-save-system.md v3.1（commit 4）

- CardEntry 新增 `techTree?: TechTreeProgress` 字段
- `baseLevel` 标 @deprecated，迁移期保留
- `CardInDeck.instanceLevel` 注释收窄（仅本局 / 仅数值 / 不持久）
- §1.2 默认存档塔卡加 techTree 初值
- §6.2 新增 v2.0.0 → v2.1.0 迁移步骤：baseLevel=N → 自动解锁路径 1 前 N 节点；`ice_tower_basic` → `elemental_tower_basic` 重命名
- `DEFAULT_SAVE.version: '2.0.0'` → `'2.1.0'`

### 1.6 同步 25-card-roguelike-refactor.md v0.6（commit 5）

- 顶部加 v0.6 变更声明 + 例外条款（塔升级权威转移至 30）
- §2.4「卡牌升级状态」：塔卡不走商店/技能卡升级，仅保留 `instanceLevel`
- §3.2 商店表：删除"[升级] 箭塔→L2"商品；新增「路径重置券」与「碎片包」商品
- §8.1 `cardLevelMap` 对塔卡废弃，引导至 13 的 `CardEntry.techTree`
- §8.2 火花碎片用途加"塔卡科技树节点 + 路径重置"
- §8.3 关内 vs 关外升级表新增"塔卡 / 非塔卡支持"列

### 1.7 同步 19-missile-tower.md（commit 6）

- 顶部 v3.1 变更声明
- §二「升级机制（结构）」标 DEPRECATED
- §4.3.1 新增「路径 1 同坐标多发射」机制详述：
  - 评分跑一次，向单地格依次发 N 发
  - 抛物线弹道独立，垂直速度小扰动形成扇形
  - 爆炸独立结算（"叠加打"而非"取最大"）
  - 错开 80-120ms 延迟仅作视觉错位
  - 与路径 2（灼烧+扩大爆炸）互斥
  - ProjectileSystem 复用现有弹道实体，循环次数取 `equippedPath.nodeIndex`

### 1.8 同步 04-skill-buff-system.md v3.1（commit 7）

- 顶部 v3.1 变更声明
- §2.2「L3 解锁」被动技能标 DEPRECATED
- §3.3 预定义 Buff 表新增 5 行：病毒感染 / 核电过载 / 激光蓄能 / 能量丹概率
- §6 新增"v3.1 塔科技树新增机制"四节：
  - §6.1 贯穿（战术炮塔）：maxPierce=3 / falloff=0.8 / 与AOE互斥 / piercedEids 防重打
  - §6.2 传染（病毒塔）：双阶段触发，半径 80 / 跳数 3 / 衰减 0.5 / Boss 免疫 / 每敌冷却 0.5s 防爆炸
  - §6.3 全屏闪电（闪电塔）：标记积累 + 5% 概率触发 + 10s CD + 1.5×伤害 / 与链式弹跳叠加非替代
  - §6.4 蓄能聚焦（激光塔路径2）：自身 self_buff / 目标切换清零 / 特变保留 50%

### 1.9 同步 21-mda-numerical-design.md v1.2（commit 8）

- 顶部 v1.2 变更声明
- §20 新增「塔科技树数值（v3.1）」整章：
  - §20.1 通用规则
  - §20.2 节点解锁成本：depth=1/2/3/4 → 300/600/1200/2400 碎片
  - §20.3 七塔 23 个节点能力数值占位
  - §20.4 路径重置：100 碎片基础费 + 70% 返还
  - §20.5 解锁基础卡 + 满级单路径总成本 ≈ 2250 碎片 ≈ 1.5-2 次 Run
  - §20.6 与 §17 经济关系
  - §20.7 调参原则与触发频率期望

### 1.10 同步 README.md v3.1（commit 9）

- 顶部加 v3.1 增量声明
- 导航表加 30 号文档
- 03 / 04 / 13 / 19 / 21 / 22 / 25 七个文档的状态列同步更新

---

## 二、关键设计决策记录

| # | 决策 | 拒绝方案 | 采纳理由 |
|---|------|---------|---------|
| 1 | 单基础塔卡 + 两路径分支可点满 + 单局装备一条 | 每条路径单独的"路径卡" | 避免卡池种类爆炸（7 塔 × 2 路径 = 14 张卡 vs 7 张基础卡） |
| 2 | 关内保留 instanceLevel（仅数值不切形态） | 关内完全禁升级 | 复用现有 v3 机制；保留法术卡"狂怀术"等关内决策深度 |
| 3 | 真火塔能量丹复用 EnergySystem +1 E | 引入新资源"能量丹" | 不破坏 v3.0 三资源轴；UI/经济链不动 |
| 4 | 弩炮塔穿透归到战术炮塔 | 直接彻底删除穿透机制 | 保留"阵列敌"解法，避免玩家工具箱缩水 |
| 5 | 路径重置返还 70% | 50% / 100% | 70% 略宽松，鼓励试错；100% 会让"路径选择"失去意义 |
| 6 | 旧 baseLevel=N 迁移为路径 1 前 N 节点解锁 | 重置玩家进度 + 全额补偿碎片 | 平滑迁移、不破坏老玩家投入感、不需要额外碎片预算 |
| 7 | 闪电塔全屏技 5% 概率 + 10s CD | 主动技能（玩家手动触发） | 与塔防"放置类"主线一致；避免引入塔的技能 UI |

---

## 三、明日待办（Phase B 启动准备）

本次设计已完整落地，接下来进入实施阶段（代码改造）：

1. **配置层**（先行）：把 30 文档的 YAML 定义写入 `config/units/towers.yaml`，新增 `techTree` 字段，删除旧 `upgrades`。
2. **存档层**：实现 v2.0.0 → v2.1.0 迁移函数（`src/save/migrations/v2_1_0.ts`），含测试覆盖。
3. **卡池 UI**：实现关外卡池界面的科技树面板（树状节点 + 路径切换按钮 + 重置按钮）。
4. **战斗层适配**：
   - `BuildSystem` 部署塔时读取 `CardInDeck.equippedPath` 并应用节点能力
   - 新增 `RuleHandlers`：`pierce_attack` / `infect_propagate` / `chain_global` / `laser_capacitor`
5. **测试覆盖**：每个节点能力一个集成测试，全屏闪电/传染需要专门的概率测试（固定种子）。

预期所有改动 = Phase B 一个子任务，约 10-15 个原子 commit。

---

## 四、风险与开放问题

- **占位数值未实测**：21 §20.3 的全部数值是"设计上合理"的初始值，实际平衡需要 Phase B 后实测调校。21 §20.7 已记录调参信号。
- **闪电塔全屏技的视觉负担**：屏幕短闪 + 多道闪电同时劈下，对低端设备性能影响未评估。Phase B 实现后需要 60fps 压力测试。
- **病毒塔传染的连锁爆炸风险**：理论最坏情况 = N 个未感染敌人聚集时，单次普攻可能触发 3 跳传染，每跳再传染 3 个，最多 1 + 3 + 9 = 13 个目标。需在 21 §20.7 监控并视情况下调 `infectJumps` 至 2。
- **30 文档 §9 五个开放问题**待 Phase B 期间收口：路径重置粒度、切换冷却、真火能量上限、闪电视觉细节、节点版本化策略。

---

## 五、提交清单

```
1. docs(design): 30-tower-tech-tree 文档新建（396 行）
2. docs(design): 22-new-unit-design v1.2 - 毒藤塔/弩炮塔废弃
3. docs(design): 03-unit-data v3.1 - 塔表与卡池索引同步
4. docs(design): 13-save-system v3.1 - CardEntry.techTree + v2.1.0 迁移
5. docs(design): 25-card-roguelike-refactor v0.6 - 塔卡升级移出商店/技能卡
6. docs(design): 19-missile-tower - 双联/集束同坐标多发射机制
7. docs(design): 04-skill-buff-system v3.1 - 加贯穿/传染/全屏闪电/激光蓄能
8. docs(numerical): 21-mda v1.2 - §20 塔科技树数值首次入库
9. docs(design): README v3.1 - 加入塔科技树（30）与同步状态
```

合计 9 个原子 commit，全部已落地 `rougeli` 分支。

---
---

> 第二主题：关卡编辑器 Phase B 推进 —— PathGraph 模型铺设与战斗代码切换
> 设计权威：[29-level-editor.md](../29-level-editor.md) §4.5（图模型路径）
> 影响代码：core/types、level/graph/、systems/、data/levels/、editor/

---

## 六、Phase B 完成进度（10 commit）

将关卡编辑器从「线性 `enemyPath: GridPos[]`」推进到「图模型 `PathGraph + spawns`」，让战斗系统全部脱离 `map.enemyPath` 的直接消费。本日推进 B.5 → B.14 共 10 个原子提交，全部 TDD + 三连验证（typecheck / npm test / build）落地。

### 6.1 提交清单（按拓扑顺序）

```
B.5  6c95f01 feat(level-graph): PathGraph TypeScript 类型骨架（+10 测试）
B.6  8d5e6cb feat(level-graph): zod schema + 11 不变量校验（+44 测试）
B.7  8bba271 feat(level-graph): 8 个 PathGraph 纯函数（+34 测试）
B.8  7ba4cbc feat(level-graph): migrateEnemyPathToGraph L1-L5 迁移函数（+21 测试）
B.9  76d1dba feat(editor): EditorRoot 加迁移按钮 + LevelEditor 迁移 API（+16 测试）
B.10 a8d2cf2 feat(types): MapConfig 扩展 spawns?/pathGraph? + loaderAdapter（+8 测试）
B.11 7eb9000 feat(wave): B.11 WaveSystem.spawnEnemy 走 resolveGraphFromMap 取 spawn 坐标（+4 测试）
B.12a cd7bb14 refactor(movement): B.12a MovementSystem 走 linearizeForLegacy 单链节点列表（+15 测试）
B.13 8462973 feat(missile): B.13 MissileTargeting 接入 resolveGraphFromMap 取 crystal_anchor（+3 测试）
B.14 36f64ee feat(levels): B.14 5 个 level fixture 添加 pathGraph + spawns 字段（+25 测试）
```

总计 +180 测试。每 commit 单独通过 typecheck + npm test + build，零回归。

### 6.2 阶段里程碑

**核心成果**：战斗系统三大消费者（WaveSystem / MovementSystem / MissileTargeting）已全部脱离对 `map.enemyPath` 的直接依赖，改读 `resolveGraphFromMap(map)` 输出的 `ResolvedGraph`：

- **WaveSystem**：`spawnEnemy` 走 `resolvedSpawns` 拿 spawn 坐标（B.11）
- **MovementSystem**：构造器内 `linearizeForLegacy` 把图退化成单链 `GridPos[]`，update 循环改读 `this.path`（B.12a）—— 极小切片，外部行为零变化
- **MissileTargeting**：`evaluateMissileTarget` 通过 WeakMap 缓存 `ResolvedGraph`，取 `crystal_anchor` 角色节点替代 `enemyPath[length-1]`（B.13）
- **5 个 level fixture**：模块顶层调用 `migrateEnemyPathToGraph` 静态派生 `pathGraph + spawns`，loaderAdapter 命中 fixture 已派生字段而非每次 migrate（B.14）

**字段双轨状态**：`MapConfig.enemyPath` 仍存活，因为：
- 类型层 required
- 5 个 yaml fixture 仍是 `enemyPath` 编码
- migration / loaderAdapter 内部 fallback 仍读 enemyPath
- 5 个 ts fixture dual-track（同时有 enemyPath 和 pathGraph）

下次工作（B.15）做最后清场：删 MapConfig.enemyPath、迁移 5 yaml、清 ts fixture 的 enemyPath，进入纯图模型。

### 6.3 关键设计决策

**B.12 拆分**：原 B.12 是 MovementSystem 真图遍历，工程量大、风险高。与用户确认后拆为：
- **B.12a 极小切片**：MovementSystem 内部 path 来源从 `this.map.enemyPath` 换成 `linearizeForLegacy(resolveGraphFromMap(map))`，单链场景下输出严格等价。仅触动构造器 + 三处 `this.path` 替换，update 循环 282 行不动。零风险落地。
- **B.12b 真图遍历**：Movement 组件 schema 改造（pathIndex 重语义为 graph.nodes 索引）、随机分支、portal 跳转、crystal_anchor 检测。**推迟到游戏关卡确有多分支/传送门需求时再做**——当前所有关卡都是单链路径，B.12a 完全够用。

**B.13 WeakMap 缓存**：MissileTargeting.evaluateMissileTarget 每帧多次调用，每次 build graph 浪费。采用 `WeakMap<MapConfig, ResolvedGraph | null>` 缓存，相同 map 引用复用 graph。`getCachedGraph` try/catch fallback null —— BehaviorTree.test.ts 的 13 个 SelectMissileTargetNode 测试用无 enemyPath fixture，需优雅 fallback 到 `MAP_ROWS-1` / `MAP_COLS-1`（与旧代码兜底等价），保留 156 测试全绿。

**B.14 零行为漂移设计**：fixture 不硬编码 graph，而是模块顶层 `migrateEnemyPathToGraph({ enemyPath: WAYPOINTS })` 派生。每次模块加载 migrate 跑一次（开销可忽略），保证 fixture 的 graph **永远等于 migration 输出**，零 mismatch 风险。TDD 测试用 `expect(map.pathGraph).toEqual(derived.pathGraph)` 锁死契约。

### 6.4 测试统计

- B.5 起 baseline：964 测试 → B.14 结束：1144 测试（+180）
- 实际我引入：180 测试
- 期间 baseline 累计变化：964 → 1119（+155，他人/并行 work）
- 1144 = 1119 + 25（我 B.14 新增）

### 6.5 多 agent 协作场景与防护

本日发现的工程纪律事件：

1. **他人 untracked WIP 被 stash pop 误带入**：在 B 段初期某次 `git stash pop` 引入了不属于本任务的 `WaveSystem.runcontext-wireup.test.ts`，作者是另一位推进 roguelike 重构的 agent。识别后**未提交也未删除**，移至 `.editor-trash/WaveSystem.runcontext-wireup.test.ts` 隔离。事后该 agent 自己提交了 `fa403ef refactor(roguelike): B2-a WaveSystem ↔ RunContext 集成层 wire-up 测试`，证实了隔离判断正确。
2. **并发 flaky 测试**：`WaveSystem.spawn.test.ts` 共享 `RenderSystem.sceneOffsetX/Y` 静态状态，并发执行时 1/3 ~ 3/5 概率失败。HEAD baseline 同样 flaky（与图模型重构无关），但每次提交前通过单独 verbose 跑该测试套件确认我没引入新失败。最终归类为「独立工程问题，与本会话工作无关」，未尝试修复以保持原子性。
3. **roguelike 并行同期落地**：B.13 与 B.14 之间，另一位 agent 落了 `0a2276c refactor(roguelike): B3-MVP SpellCastSystem 一张火球卡贯通全链路`，新增 ~60 测试。我的 rebase 行为：每次 commit 前 `git fetch` + 检查 HEAD，发现差异立即评估冲突区域。本日 10 commit 与 roguelike 改动**零冲突**——因为我严格保持触动文件范围最小化（B.10 例外，因 MapConfig 扩展属于类型层），充分体现了「设计先行 + 原子提交」的协作保护作用。

### 6.6 Phase B 余量

- **B.12b** MovementSystem 真图遍历（等待多分支/portal 关卡需求）
- **B.15** 删除 enemyPath 字段（收尾，约 10-15 个文件改动）
- **yaml fixture 迁移**：5 个 yaml 改成 pathGraph 编码（可与 B.15 合并）

下次工作起点：HEAD `36f64ee`，分支 `rougeli`。

---

## 七、风险与开放问题（Phase B 视角）

- **B.12b 实际需求未达**：所有现役关卡都是单链路径，真图遍历做了也用不上。等 design/29 §4.5.8 出现「这关需要敌人在分叉处随机走」或「这关需要传送门」的具体玩法设计后再实施，避免过度工程。
- **flaky 测试治理**：`WaveSystem.spawn.test.ts` 的 RenderSystem 静态状态污染需要独立工程清理，建议作为单独 chore commit。已记录在本日志，下次会话决定是否处理。
- **enemyPath 字段死期**：B.15 删除时会触动 12+ 文件（5 ts fixture + 5 yaml + MapConfig type + migration + loaderAdapter），需谨慎排序避免破坏构建。

---

## 八、B.15 enemyPath 字段移除（2026-05-14 当日收尾）

### 8.1 启动状态

进入本节时工作树 dirty：上一会话末点开过编辑器"迁移+保存"按钮，落盘了 5 个 yaml 的 pathGraph+spawns 编码、删了 enemyPath、yaml dump 块风格、丢失原注释。已审计与 HEAD 的等价：L1-L4 tiles/waves 完全等价、节点数=原 enemyPath 拐点数；L5 保留数字 tile 编码。决策：**接受 dirty 作为 B.15.0 起点**。

### 8.2 三次原子提交

**B.15.0 — yaml fixture 落盘 + migration 测试改写**
- 5 个 `src/config/levels/level-0{1..5}.yaml` 已带 pathGraph+spawns、不再含 enemyPath
- 重写 `src/level/graph/__tests__/migration.test.ts` 中 "L1-L5 真实 YAML 保真" describe → "L1-L5 真实 YAML 内嵌 pathGraph 合规验证（B.15）"：从读 enemyPath 改为读 pathGraph+spawns，校验 schema + 单链 chooseNext 可达所有节点
- 5/5 migration 测试由失败转为通过

**B.15.1 — main.ts 切换 base 实体坐标源**
- `src/main.ts` L336：base 实体坐标不再读 `map.enemyPath[length-1]`，改走 `resolveGraphFromMap(map)` 找 role=crystal_anchor 的节点（同 MissileTargeting.ts:101 现成模式）
- crystal_anchor 缺失时回退到 nodes[length-1]（迁移函数永远把 enemyPath tail 转成 crystal_anchor，行为等价）

**B.15.2 — 一次性彻底删字段（方案 A 原子提交）**
用户选择方案 A：单次 commit 同时改 18 文件，每步 typecheck 全绿。范围：
- `src/types/index.ts`：删 `enemyPath` 字段，不保留 `@deprecated` 过渡
- 5 个 ts fixture（`level-0{1..5}.ts`）：删 `enemyPath: WAYPOINTS` 字段，`PATH_GRAPH/SPAWNS` 仍由 `migrateEnemyPathToGraph(WAYPOINTS)` 派生
- `src/data/gameData.ts` MAP_01：同步转 pathGraph+spawns（MissileTargeting.test/ProjectileSystem.test 仍消费）
- `src/level/graph/loaderAdapter.ts`：删 enemyPath fallback 分支，缺失 pathGraph 直接抛错
- 6 个测试：
  - `loaderAdapter.test.ts` 重写——只保留 "pathGraph+spawns 完备返回"、"缺失 pathGraph/spawns 抛错" 3 个质量门
  - `WaveSystem.spawn.test.ts`：legacy 路径走 `migrateEnemyPathToGraph` + pathGraph；错误路径改为 'pathGraph missing'
  - `MissileTargeting.crystalAnchor.test.ts`：`makeMapWithEnemyPath` → `makeMapWithWaypoints`（内部 migrate）
  - `levelFixtures.pathGraph.test.ts`：从 B.14 "与 enemyPath 派生一致" → B.15 "pathGraph+spawns 自洽性"（节点数 ≥2、唯一 crystal_anchor、spawns row/col 对齐 spawn 节点、edges 只引现有 node id）
  - `BehaviorTree.test.ts` / `BuildSystem.cost-decoupling` / `WaveSystem.runcontext-wireup` / `MovementSystem.test` / `MovementSystem.pathEquivalence` / `grid.test`：6 文件批量把 `enemyPath: [...]` 换成 "spawn 头节点 + crystal_anchor 末节点" 最小 pathGraph

### 8.3 验证状态

- `npm run typecheck`：全绿
- `npm test --run`：1138/1144 passed，6 个 fail 全部位于 `WaveSystem.spawn.test.ts`，**单跑 3/3 全绿** —— 与已知 RenderSystem 静态状态 baseline flaky 一致，与 B.15 改动无关
- `migration.ts` 函数本身**保留**，作为 yaml/ts 迁移工具（上次会话编辑器仍依赖）

### 8.4 形态变化

B.15 后：
- `MapConfig.enemyPath` **字段不存在**，任何 fixture / 测试 / 编辑器输出都必须直接声明 `pathGraph + spawns`
- `resolveGraphFromMap` 是唯一的运行时入口，要求 map 同时携带 `pathGraph` 和 `spawns`，否则抛错
- 编辑器要把"老格式 enemyPath"导入时，仍可调用 `migrateEnemyPathToGraph` 在 UI 侧完成转换后再写入 yaml；运行时不再 forgive 老格式

### 8.5 下次工作起点

HEAD 在 `rougeli`（领先 origin/rougeli 6 commit，未推送），分支处于"Phase B 收尾完成"状态，Phase C 编辑器 UI 化（design/29 §6.2-§6.6 节点 GUI / 波次 GUI）尚未启动。

---

## 九、design/ 文档结构彻底重构（R0–R6，2026-05-14 下半场）

### 9.1 背景

v3.0 卡牌化 + v3.1 塔科技树两轮玩法变更后，`design/` 顶层堆积 31 份扁平 `.md` 文件，存在 M1–M7 七处玩法冲突 + 两份单位文档（旧 03/22）功能重叠 + 命名混乱（旧 ice_tower / 已删毒藤塔/弩炮塔仍在多处出现）。用户要求"彻底重构"。

### 9.2 方案

按职责切分 7 个子目录（00-vision / 10-gameplay / 20-units / 30-ai / 40-presentation / 50-data-numerical / 60-tech）+ archive/ + dev-logs/，每篇文档强制 YAML frontmatter，标 status（authoritative / stable / archived）+ authority-for（领域所有权）+ cross-refs。改造分 6 阶段，每段独立 commit。

### 9.3 阶段执行结果

| Phase | Commit | 描述 | 影响 |
|---|---|---|---|
| R0 | `9caffcc` | baseline 三文件（refactor-plan / refactor-acceptance / _template） | 落地重构契约 |
| R1 | `57a5900` | 7 层目录 + archive/ 创建 + 两份零散研究笔记归档 | 目录骨架 + archive 启用 |
| R2 | `353b0a2` | M1–M7 矛盾内容清理 | 9 file, +274/-436 |
| R3 | `775be8d` | 合并 旧 03 + 旧 22 → `20-units/21-unit-roster.md`（lean-merge 策略：保 schema + 阵容 + 卡牌目录，数值全 → 50-mda） | 8 file, -1274/+543（净 -731） |
| R4 | `ac451de` | 28 文件 `git mv` 入新目录 + 28 份 frontmatter 注入 + 28 份 H1 去旧编号前缀 + 301 内部链接 batch 重写 + 7 处 R3 遗留链接手补 + README 45 处链接同步 | 31 file, +712/-316 |
| R5 | `19a94e4` | 8 份"沿用类"文档追加固定 4 行 v3.1 一致性核对表 + 修订历史；44-visual-effects.md 删除残留毒藤塔/弩炮塔视觉行 | 8 file, +171/-4 |
| R6 | （本提交） | README.md 全文重写为分层导航；R0–R6 总结写入本日志；删除 refactor-plan.md + refactor-acceptance.md（任务完成） | 4 file |

### 9.4 度量

- **文档总数**：31 → 29（合并 -2）+ 4 archive + N dev-logs；顶层 `design/*.md` 从 31 缩到 **2**（README.md + _template.md），满足 G1（≤5）。
- **内部链接**：经 R4/R5/R6 三轮重写，最终活跃 markdown 链接 332 条（排除内联代码示例），**断链 0**（机器校验：path-aware Python 工具，遵循 `./`、`../` 相对前缀，忽略 `archive/` 与 `dev-logs/` 内部历史链接）。
- **frontmatter 覆盖率**：所有 29 份 layered 文档 100% 带 YAML frontmatter（title / status / version / last-modified / authority-for / supersedes / cross-refs）。
- **authoritative 文档共 5 篇**：10-roguelike-loop / 22-tower-tech-tree / 50-mda / 60-architecture / 64-level-editor。

### 9.5 M1–M7 决议落地

| ID | 主题 | 决议 | 落地位置 |
|---|---|---|---|
| M1 | instanceLevel 提升路径 | 仅法术卡可升；3 层上限；塔死亡 / 关卡结束清零 | `20-units/23-skill-buff.md` §7 |
| M2 | L3 被动技能 | 全部废弃，能力承接到塔科技树节点；原表存入 `archive/deprecated-l3-passives.md` | archive/ 落地 |
| M3 | 存档迁移 | 游戏未发布，无需迁移代码；存档恒为 v2.0.0；`baseLevel` 字段直接删除（不留 @deprecated 占位） | `60-tech/61-save-system.md` §6 |
| M4 | 天气影响范围 | 仅战斗数值，不影响经济 | `10-gameplay/14-weather.md` §3 + R5 audit 表 |
| M5 | 士兵升级 | path 科技树延后，旧 §12 标 TODO，代码字段保留为占位 | `30-ai/31-soldier-ai.md` §12 |
| M6 | 沿用类文档审计 | 8 份文档强制追加固定 4 行一致性核对表 | R5 一次性完成 |
| M7 | 卡系统权威源 | `10-gameplay/10-roguelike-loop.md` 是唯一来源；20-unit-system §8 / 21-unit-roster §7 都是 cross-ref | R2 边界声明 |

### 9.6 重命名 & 废弃落地

- `ice_tower` → `elemental_tower`（默认冰形态，路径覆盖冰/火/毒）：21-unit-roster.md 全部使用新 id；"冰塔"作为非正式显示名仍可出现，指默认形态。
- `vine_tower` / `ballista_tower`：全部删除，仅 `archive/deprecated-units-vine-ballista.md` 保留原设计；44-visual-effects.md §4.1.2/§4.1.3 残留行在 R5 清除。

### 9.7 后续遗留

R6 完成后剩余三个非阻塞性 v3.1 缺口（标 🔶，**不在本次重构范围**，留给后续 PR 专项处理）：

1. `60-tech/63-debug.md`：调试入口仅支持金币 +99999，缺能量 / 火花碎片 / 科技节点解锁快捷入口。
2. `30-ai/31-soldier-ai.md` §12：士兵 path 科技树设计本身尚未启动，仅有 TODO 标记。
3. 其它 audit 表中标 🔶 的若干小节，多为命名一致性确认，无设计缺陷。

### 9.8 提交链（HEAD = R6）

```
R6: refactor(docs): R6 — README 重写 + dev-log 总结 + 删除 refactor 流程文件
R5: refactor(docs): R5 — 沿用类文档追加 v3.1 一致性核对（19a94e4）
R4: refactor(docs): R4 — 28 docs migrated to 7-layer structure + frontmatter（ac451de）
R3: refactor(roguelike): R3 合并 03+22 → 20-units/21-unit-roster.md（775be8d）
R2: refactor(roguelike): R2 清除 M1-M7 矛盾内容（353b0a2）
R1: refactor(roguelike): R1 创建分层目录骨架（57a5900）
R0: refactor(roguelike): R0 重构基线提交（9caffcc）
```

文档重构闭环完成。后续设计变更应直接在分层目录中提交，遵循 `_template.md` 与 README "文档贡献规范"。

