# 开发日志 — 2026-05-12

## 蝙蝠塔攻击动作设计

**背景**：蝙蝠塔（Bat Tower）的攻击架构与其他塔完全不同——攻击由独立的蝙蝠群成员（Bat Swarm Member）实体执行。当前 `BatSwarmSystem.tryAttack()` 中仅有数据层面 `applyDamageToTarget()` + 目标 `hitFlashTimer`，蝙蝠自身无任何攻击动画反馈。

**工作内容**：
- 深入分析了蝙蝠塔全链路实现：bitecs 组件定义、建造流程、蝙蝠实体创建、攻击逻辑、boid 集群运动、复合渲染、AI 行为树
- 梳理了现有 7 种攻击动画模式：受击闪白、临时特效实体、持续连线、屏幕震动、状态效果、陷阱伸缩、蝙蝠翼拍悬浮
- 设计并写入四阶段攻击动作方案到设计文档

**设计决策**：

| 阶段 | 持续时间 | 核心视觉 |
|------|---------|---------|
| 锁定蓄力 | ~0.27s (20%) | 翼展加大、暗影粒子凝聚、转身对准目标 |
| 俯冲攻击 | ~0.53s (40%) | 贝塞尔弧线飞行、高频拍翅、暗影拖尾粒子 |
| 撕咬命中 | ~0.20s (15%) | 红色獠牙绘制、命中粒子爆散、绿光吸血回流、屏幕微震 |
| 回弹归巢 | ~0.33s (25%) | 弹回 boid 位置、翅膀减速、绿色吸血余光淡出 |

关键设计点：
- 伤害延迟到 phase=0.65（撕咬瞬间）而非攻击启动即刻——让玩家看到因果链
- 攻击期覆盖 boid 位置计算，避免集群算法干扰攻击轨迹
- 弧高 15-30px 随机，让多只蝙蝠同时攻击时轨迹不重叠
- 复用现有 ParticleContainer / BloodParticleSystem 粒子池，不引入新渲染系统

**修改文件**：
- `design/12-visual-effects.md` — 新增 §9 蝙蝠塔攻击动作设计（~200行）
- `design/16-art-assets-design.md` — 更新攻击动画表蝙蝠塔行，引用新 §9

---

## 验收修复：基地不掉血 + 起始金币对齐

**背景**：用户验收反馈两个问题——「开局金币太少了无法安放任何单位」、「小兵攻击基地基地不掉血」。

**问题 1 诊断（基地不掉血）**：
深入排查 MovementSystem.onReachEnd 中读取 `Attack.damage[eid] ?? 10` 永远取 0 的现象。根因是 bitecs `Types.f32` TypedArray 的固有行为——未对 entity addComponent(Attack) 时，TypedArray 默认值为 0 而非 undefined，导致空值合并 `??` 永远不触发回退。Grunt 等近战敌人（`attackRange=0` 不挂 Attack 组件）走到终点造成 0 伤害。

**问题 2 诊断（金币不足）**：
对照 `design/21-mda-numerical-design.md` §7.1 起始金币表，发现 4 个关卡配置与权威表不一致：L1 少 20G、L2 少 10G、L4 多 10G、L5 多 20G。

**修复方案**（TDD 流程）：

| 阶段 | 工作 |
|------|------|
| 红 | 先写 7 个 MovementSystem 回归测试：近战敌人扣血、远程敌人扣血、不同 atk 值、Boss、自伤保护、destroy 时机、无基地早返。运行确认 5/7 红。 |
| 绿 | UnitTag 新增 `atk: Types.f32` 字段，WaveSystem.spawnEnemy 时统一写入 `config.atk`，MovementSystem.onReachEnd 改读 `UnitTag.atk[eid]`。再跑测试 7/7 全绿。 |
| 对齐 | 修改 level-01/02/04/05.ts 起始金币至文档值（220/260/340/380）；同步修正 design/07 示例值。 |
| 回归 | `npm run typecheck` 通过；`npm test` 全量 366/366 绿（基线 359 + 新增 7）。 |

**关键设计决策**：
- 选择 UnitTag.atk 作为伤害唯一来源（而非新增专用组件或保留 Attack 双源），理由：所有敌人都有 UnitTag，数据契约统一；spawn 时一次写入，避免 onReachEnd 路径再 lookup `ENEMY_CONFIGS[type].atk` 的运行时开销。
- 在 MovementSystem.onReachEnd 留下 docstring 警告，明确告知未来重构者「不要回退到 `Attack.damage[eid] ?? 0`」——bitecs TypedArray 默认 0 是已知陷阱，避免回归。

**修改文件**（两个原子提交）：
- 提交 1 `fix(combat)`：`src/core/components.ts`、`src/systems/WaveSystem.ts`、`src/systems/MovementSystem.ts`、`src/systems/MovementSystem.test.ts`（新）。
- 提交 2 `chore(balance)`：`src/data/levels/level-{01,02,04,05}.ts`、`design/07-map-level-system.md`。

---

## P3 R5 — TOWER_MISSILE_AI v1.0 三节点接管完成（晚）

**承接**：P3 R1（接口冻结 e1dae9e）→ R2（SelectMissileTargetNode 8196bd1）→ R3（ChargeAttackNode 26f3b57）→ R4（LaunchMissileProjectileNode 3280623）。

**R5 切换工作**：

| 项 | 文件 | 内容 |
|----|------|------|
| AI 配置升级 | `src/ai/presets/aiConfigs.ts` | TOWER_MISSILE_AI 从 0.1-stub 升 v1.0：`selector{ sequence(select_missile_target → charge_attack → launch_missile_projectile), wait(0.1) }` |
| Cooldown 守卫 | `src/ai/BehaviorTree.ts` ChargeAttackNode | Phase A 增加 `Attack.cooldownTimer > 0 → FAILURE`，解决 BT 每帧 tick 与 cooldown 解耦导致的重复蓄力 bug；与 AttackSystem.update 冷却语义等价 |
| 单测增量 | `src/ai/BehaviorTree.test.ts` | +1 cooldown 守卫 FAILURE 用例（13 套 384/384 全绿） |
| Provider 注入 | `src/main.ts` | `aiSystem.setMapConfigProvider(() => this.currentMap)`，SelectMissileTargetNode 依赖此 provider 算敌人距基地距离 |
| AttackSystem 薄化 | `src/systems/AttackSystem.ts` | `handleMissileTower` 薄化为 no-op + `@deprecated` docstring；line 158-161 dispatch 保留（结构兼容）；清理 `evaluateMissileTarget` / `MISSILE_CHARGE_TIME` / `RenderSystem` 等无用 import |

**用户脱产外修复（独立 commit 34f1074）**：
- BuildSystem 给 tower 实体补 UnitTag 组件 — 这是 R5 的隐藏前置依赖：`AISystem.aiQuery = defineQuery([AI, Position, Health, UnitTag])` 要求 tower 必须有 UnitTag，否则 BT 永不 tick，missile 塔会彻底瘫痪。

**提交清单**：
- `b7217ef` feat(bt): P3 R5 TOWER_MISSILE_AI v1.0 三节点接管 + handleMissileTower no-op
- `24ed5b9` docs(bt): P3 R5 收尾同步 design/23 §0.7/§0.8/§1.2 状态

**验证**：typecheck + 13 套 384/384 测试 + `npm run build` 全绿。

**里程碑**：**15/15 AI 配置全部达 v1.0**。导弹塔三节点（select → charge → launch）成为 BT 接管复杂战术单位的范例 —— BT 描述时序，MissileCharge 组件作通信总线，ProjectileSystem 负责物理。

---

## P3 R6 — 烟雾测试验证（Playwright MCP 端到端）

**目的**：R5 单测全绿后，验证生产 dev 环境中导弹塔 BT 实际行为。

**步骤**：
1. `npm run dev` 启动 port 3000，导航 `/ai-tower-defender/`。
2. 通过 console eval 注入 9999 gold，调用 `buildSystem.placeTower(world, x, y, row, col)` 在 row=4, col=10 放下 missile 塔（type='missile'）。
3. 等 3s 让 BT 跑多个完整 select→charge→launch 周期。
4. 通过 `import('/src/core/components.ts')` 读 ECS 组件 SoA 数组，检查塔状态与发射结果。

**关键观测值**：

| 指标 | 实测值 | 期望 | 结论 |
|------|--------|------|------|
| `Tower.towerType[23]` | 6 | 6 (missile) | ✅ |
| `AI.configId[23]` | **15** | 15 (TOWER_MISSILE_AI) | ✅ AI 配置 ID 正确 |
| `Attack.cooldownTimer[23]` | **7.14s** | 7s × 1/attackSpeed≈7.14 | ✅ LaunchMissileProjectileNode 重置 cooldown 生效 |
| `MissileCharge.chargeElapsed/markId` | 0 / 0 | 0（cooldown 期间无蓄力） | ✅ ChargeAttackNode R5 新增 cooldown 守卫工作 |
| 活跃 missile projectile 数 | **8** | ≥1 | ✅ 全流程贯通 |
| projectile.damage / splash | 90 / 130 | L1 base 90 / AOE 130 | ✅ 符合 21-mda §6.2 |
| 活跃 TargetingMark 数 | 8 | =projectile 数 | ✅ ChargeAttackNode spawn mark 正确 |
| `aiSystem.totalUpdates` | 12616 | 持续递增 | ✅ BT tick 正常 |
| console errors | 0 | 0 | ✅ |

**结论**：R5 在 dev 运行时 100% 验证通过，导弹塔放置后立刻通过 BT 完成完整周期，cooldown 守卫正确阻止重复蓄力。

**提交**：`4e6c432` docs(dev-log) — P3 R5 落地记录已纳入；本次 R6 smoke 记录可一并续记。
